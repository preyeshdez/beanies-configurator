{
  "name": "beanies",
  "version": "1.0.0",
  "private": true,
  "description": "Prototipo de configurador web de beanies personalizados para ecommerce",
  "scripts": {
    "install:all": "npm install --prefix configurator-api && npm install --prefix ecommerce-api",
    "configurator": "npm --prefix configurator-api start",
    "ecommerce": "npm --prefix ecommerce-api start",
    "dev": "concurrently -n CONFIG,ECOM -c blue,green \"npm run configurator\" \"npm run ecommerce\"",
    "seed:templates": "npm --prefix configurator-api run seed:templates",
    "seed:colors": "npm --prefix configurator-api run seed:colors"
  },
  "devDependencies": {
    "concurrently": "^10.0.1"
  }
}